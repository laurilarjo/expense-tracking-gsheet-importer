import { LOGIN_URL } from "../../config";

const main = () => {
  return (
    <main className="flex items-center justify-center">
      <a href={LOGIN_URL}>Login</a>
    </main>
  );
};

export default main;
